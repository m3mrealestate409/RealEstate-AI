# Secrets
.env
*.env.local

# Local operator cheat-sheet: contains this deployment's live URL and machine
# paths. Stays on the server/laptop, never in the public repo.
COMMANDS.txt

# Marketing/landing website — kept isolated from the RAG app and out of this
# public repo. Gets its own (private) repo when it's ready to ship.
marketing-site/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Uploads / data
backend/uploads/
*.sqlite3

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node (future frontend)
node_modules/
dist/
build/

# User-uploaded assistant avatars (runtime data, not source)
backend/app/static/avatars/
